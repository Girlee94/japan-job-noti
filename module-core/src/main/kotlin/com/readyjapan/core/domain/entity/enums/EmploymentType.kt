package com.readyjapan.core.domain.entity.enums

/**
 * 고용 형태
 */
enum class EmploymentType {
    /** 정규직 (正社員) */
    FULL_TIME,

    /** 계약직 (契約社員) */
    CONTRACT,

    /** 파트타임 (パートタイム) */
    PART_TIME,

    /** 인턴 (インターン) */
    INTERN,

    /** 프리랜서 (フリーランス) */
    FREELANCE
}
